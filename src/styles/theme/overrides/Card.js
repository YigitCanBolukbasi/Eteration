const card = (theme) => ({
  MuiCard: {
    styleOverrides: {
      root: {
        borderRadius: "0px",
        maxWidth: 345,
        // height: "180.95px",
        // overflow: "scroll",
      },
    },
  },
});

export default card;
