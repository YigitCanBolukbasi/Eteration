const paper = (theme) => ({
  MuiPaper: {
    styleOverrides: {
      root: {
        borderRadius: "0px",
        // height: "180.95px",
        // overflow: "scroll",
      },
    },
  },
});

export default paper;
